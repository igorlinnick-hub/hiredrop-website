# design-day-theme — тёплый день аппа, веер, кнопки

Обновлено: 2026-09-12 · ветки: `design/white-ground-fan-cards` + `design/flow-sidebar-nav`

## Состояние

В проде тёплый день (#142/#144/#149/#152/#153). Поверх него две НЕ смерженные ветки,
выкатываются одним заходом:
- `design/white-ground-fan-cards` — белый фон страницы, веер внутри карточек Auto/Tap;
- `design/flow-sidebar-nav` (поверх первой, сборка зелёная) — **Flow-рейка**: скрины Игоря
  дошли, вместо горизонтальных пилюль на lg+ вертикальная рейка слева 236px (лого сверху,
  Dashboard/History/Platforms рабочим блоком, Settings/Extension кластером внизу, стекло
  surface/75+blur); топ-бар держит только аккаунт-контролы; мобилка < lg остаётся на пилюлях.
  День: активный пункт = плитка `--accent-light` + ЧЁРНЫЙ текст + янтарная иконка (правило
  «два золота спорят»), скоуп-оверрайд в globals.css (`.hd-sidenav/.hd-mobilenav .nav-active`);
  ночь: прежняя accent/10 + гало.

Борд «было/стало» (день/ночь/мобилка, с линейками, проба getComputedStyle пройдена):
https://claude.ai/code/artifact/328a6e81-e876-4b31-8b7a-0241c619f4ee

## Последний заход

- Разобрал скрины: референс = вертикальное меню Flow, «выстелка в длину» = наш ряд пилюль.
- Переписал `DashboardLayout.tsx` (рейка + мобильные пилюли из одного рецепта классов).
- Дневной оверрайд активного пункта в globals.css; ночь не тронута.
- Превью без логина: временная страница `app/dev-nav-preview` (удалена, не в коммите),
  активный пункт подсвечивал руками через JS — pathname превью не матчится с nav.
- Прод-сборка зелёная, ветка запушена.

## Сломано / не доделано

- Активность считается `pathname === href` — подстраницы (/dashboard/tap) не подсвечивают
  ничего; так было и до рейки, но в вертикальном меню это заметнее.
- Скрины превью — с плейсхолдер-контентом: реальные карточки требуют логина (куки Игоря).

## Следующий шаг

Игорь смотрит борд → по «мержим» слить `design/flow-sidebar-nav` (несёт в себе white-ground)
в main одним PR.
