# design-day-theme — тёплый день аппа, веер, кнопки

Обновлено: 2026-09-14 · ветки: `design/flow-sidebar-nav` (website) + `design/popup-day` (jobflow)

## Состояние

В проде тёплый день (#142/#144/#149/#152/#153). Поверх него две НЕ смерженные ветки,
выкатываются одним заходом:
- `design/white-ground-fan-cards` — белый фон страницы, веер внутри карточек Auto/Tap;
- `design/flow-sidebar-nav` (поверх первой, сборка зелёная) — **Flow-рейка**: на lg+
  вертикальная рейка 236px (лого сверху, Dashboard/History/Platforms рабочим блоком,
  Settings/Extension внизу, стекло surface/75+blur); топ-бар держит только аккаунт-контролы;
  мобилка < lg на пилюлях. День-актив = плитка `--accent-light` + ЧЁРНЫЙ текст + янтарная
  иконка (скоуп-оверрайд `.hd-sidenav/.hd-mobilenav .nav-active`). **+ ночь 09-14** (тот же
  бранч): фон страницы = СВОЯ картинка `bg/ground-night.jpg` (U7 из списка daytwin — «одна
  картинка и в карточках, и фоном» Игорь забраковал); серые панели/пилюли ночью = чёрное
  стекло (`.dark .bg-surface{rgba(8,8,15,.6)}` и surface2), thead/меню почти непрозрачные;
  счётчики вкладок Job Listings белые (`.jt-count`); `.dark .hd-glass` тоже чёрное стекло
  (был непрозрачный фиолетовый градиент) — секция кампании и все hd-glass-карточки
  пропускают обои. Обе темы кампании сняты на борд.

Плюс **попап расширения — день** (Игорь 09-14: «белый, чёрные кнопки, картинка ночная»):
ветка `design/popup-day` в репо **jobflow** (запушена, на Рабочий стол НЕ синкана). Белый фон,
тёплые нейтрали, все кнопки = один чёрный рецепт, ночной веер картинкой внутри карточки
Live process (blur+вуаль, светлый текст); popup.js не тронут. Бывшее правило «popup =
dark-only» этим отменено. Выкат: «ок» → `scripts/sync-ext.sh` → тумблер OFF/ON.

Борд «было/стало» (рейка день/ночь/мобилка + попап, с линейками):
https://claude.ai/code/artifact/328a6e81-e876-4b31-8b7a-0241c619f4ee

## Последний заход

- Разобрал скрины: референс = вертикальное меню Flow, «выстелка в длину» = наш ряд пилюль.
- Переписал `DashboardLayout.tsx` (рейка + мобильные пилюли из одного рецепта классов).
- Дневной оверрайд активного пункта в globals.css; ночь не тронута.
- Превью без логина: временная страница `app/dev-nav-preview` (удалена, не в коммите),
  активный пункт подсвечивал руками через JS — pathname превью не матчится с nav.
- Прод-сборка зелёная, ветка запушена.

## Сломано / не доделано

- Активность считается `pathname === href` — подстраницы (/dashboard/tap) не подсвечивают
  ничего; так было и до рейки, но в вертикальном меню это заметнее.
- Скрины превью — с плейсхолдер-контентом: реальные карточки требуют логина (куки Игоря).

## Следующий шаг

Игорь смотрит борд → «мержим» = слить `design/flow-sidebar-nav` (несёт white-ground) в main
website; «ок» по попапу = слить `design/popup-day` в main jobflow + sync-ext + OFF/ON.
